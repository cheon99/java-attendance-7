비 효율적인 그러니까 불필요하게 늘어진 코드를 압축했다 
예를들어 
MenuAction check = new CheckAttendance(today, inputView, outputView, attendance);
actions.put(Menu.CHECK, check);
->
actions.put(Menu.CHECK, new CheckAttendance(today, inputView, outputView, attendance));
또
List<Student>[] listArr = new List[3];
listArr[0] = EXPULSION;
listArr[1] = MEETING;
listArr[2] = WARN;
->
List<List<Student>> listArr = List.of(expulsion, meeting, warn);
그리고 inputView를 전달받고 쓰지 않는데 객체를 기계적으로 전달하고 있었던 부분을 삭제했다
등

아직 검증을 담당하는 객체는 못만들었다

결과를 전달하는 도메인용 객체도 추가했다 
먼저 기존의 더티코드인 uicontroller를 전달받아 exitProgram에서 실행하는 흐름 혹은 스태틱메서드를 활용하는 흐름제어를 
MenuAction 의 run 메서드가 항상 continue나 exit를 가지는 actionResult라는 객체를 반환하도록 만들었다 
이제 흐름제어를 위해 객체를 전달하거나 스태틷메서드를 만들 필요가 없다
또 PenaltyResult라는 검사 결과 정보들을 가지는 객체를 하나 만들었고
이걸 만드는 책임은 (징계 상태를 검사하는 책임) Student에 넘겼다
다만 Student별로 반복시키는 책임은 Attendance에 넣을 수도 있었겠지만
기능 3인 학생 별 출력에서 어차피 Studnet의 메서드로 접근해야 하기 때문에 큰 효용을 못느낄것 같아 만들지 않았다 
이렇게 해서 InputView와 checkNeedWarnedStudent 기능에 둘다 비슷한 일을 하던 걸 분리할 수 있었다
다만 결과인 PenaltyResult가 항상 전부 쓰이지 않아서 조금 무겁다고 생각한다 필요한 만큼만 전달하는 게 아닌 결과를 알려주는 책임을 전부 지고 있다 보니 그런 것 같다

그리고 학생의 출석기록을 보여주는 기능에서는 등교시간이 필요했다
내가 아는 방법에 Map은 날짜와 상태 객체 하나만 매칭시킬수 있어서 어쩔 수 없이 attendance를 다시 받아서 시간을 가져오고 없다면 정해진 문자열을 출력해야했다
분리는 했지만 분리된 공통 부분으로 전부 감당할 수 없었다 
결국 outputView에 로직이 남아있다

아 그리고 없었던 논리를 기존에 우겨 넣으면서 
등교시간이 존재하지 않는 날의 AttendanceStatus를 생성해야 했는데 정책을 담당하는 부분에서 time == null인경우를 다루지 않아서 아쉬웠다 
분명한 논리적 허점이었다 

잠깐만 지금 생각해보니 AttendanceRecord가 Status도 가지고 있다 내가 굳이 AttendanceStatusLog를 만들어야 했을까?
좋아 침착하게 리팩터링 해보자 2차로 이어서